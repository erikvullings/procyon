/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { DocumentSummarySelectionModeDto } from './documentSummarySelectionModeDto.ts';
import type { SummaryKeyPassageDto } from './summaryKeyPassageDto.ts';
import type { SummaryProfileDisclosureDto } from './summaryProfileDisclosureDto.ts';

/**
 * Confirmation preview. Without a profile, passages remain local and are
 * deliberately labelled as key passages rather than a generated summary.
 */
export interface DocumentSummaryPreviewDto {
  /** Whether the selected model and document support bounded image input. */
  imageInputAvailable: boolean;
  /**
     * Embedded document images that will be sent to a verified vision model.
     * @minimum 0
     */
  includedImageCount: number;
  /** Selected complete source passages. */
  keyPassages: SummaryKeyPassageDto[];
  /**
     * Embedded images omitted because of model capability or safety budgets.
     * @minimum 0
     */
  omittedImageCount: number;
  profile?: null | SummaryProfileDisclosureDto;
  /**
     * Conservative selected-input token estimate.
     * @minimum 0
     */
  representativeTokens: number;
  /** Whether persisted support metadata was reused. */
  reusedSelection: boolean;
  /** Stable confirmation fingerprint. */
  selectionFingerprint: string;
  /** Whether the preview contains the complete extracted text or selected passages. */
  selectionMode: DocumentSummarySelectionModeDto;
}
