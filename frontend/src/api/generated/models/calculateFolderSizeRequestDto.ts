/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { LocationDto } from './locationDto.ts';

/**
 * Requests a directory's recursive total size (`POST /api/v1/directories/size`), for the Total
 * Commander-style "press a key on a folder to see how much space it consumes" behaviour
 * (task 0071).
 */
export interface CalculateFolderSizeRequestDto {
  /** The directory to sum. */
  location: LocationDto;
}
