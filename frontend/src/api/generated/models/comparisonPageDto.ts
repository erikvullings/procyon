/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { ComparisonCriteriaDto } from './comparisonCriteriaDto.ts';
import type { ComparisonEntryDto } from './comparisonEntryDto.ts';
import type { LocationDto } from './locationDto.ts';

/**
 * A bounded, optionally differences-only page of a comparison's results
 * (`GET /api/v1/comparisons/{comparisonId}`).
 */
export interface ComparisonPageDto {
  /** The comparison this page belongs to. */
  comparisonId: string;
  /** How entries were classified. */
  criteria: ComparisonCriteriaDto;
  /** Entries in this page. */
  entries: ComparisonEntryDto[];
  /**
     * Whether the backend has stopped producing further entries (either
     * finished or cancelled).
     */
  isComplete: boolean;
  /** Root compared as "left". */
  left: LocationDto;
  /**
     * Requested page size after server-side clamping.
     * @minimum 0
     */
  limit: number;
  /**
     * Requested zero-based offset, into the (possibly filtered) result set.
     * @minimum 0
     */
  offset: number;
  /** Root compared as "right". */
  right: LocationDto;
  /**
     * Number of entries matching the requested filter, known so far.
     * @minimum 0
     */
  total: number;
  /**
     * Cumulative count of unreadable directories skipped so far.
     * @minimum 0
     */
  warningsCount: number;
}
