/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { SemanticEmbeddingNormalizationDto } from './semanticEmbeddingNormalizationDto.ts';
import type { SemanticProfileDto } from './semanticProfileDto.ts';
import type { SemanticReindexEstimateDto } from './semanticReindexEstimateDto.ts';

/**
 * Expert-mode local model import plus its mandatory migration disclosure.
 */
export interface ImportSemanticLocalModelRequestDto {
  /**
     * Embedding dimensions.
     * @minimum 0
     */
  dimensions: number;
  /** Full-reindex work disclosed before confirmation. */
  estimate: SemanticReindexEstimateDto;
  /**
     * Estimated installed disk bytes.
     * @minimum 0
     */
  estimatedDiskBytes: number;
  /**
     * Estimated peak RAM bytes.
     * @minimum 0
     */
  estimatedRamBytes: number;
  /** Declared language coverage. */
  languageCoverage: string[];
  /** Human-readable license notice. */
  licenseNotice: string;
  /** SPDX license expression. */
  licenseSpdx: string;
  /** Opaque model identifier. */
  modelId: string;
  normalization?: null | SemanticEmbeddingNormalizationDto;
  /** Abstract profile assigned to the imported model. */
  profile: SemanticProfileDto;
  /** Required logical runtime component. */
  runtimeComponentId: string;
  /** Accepted runtime semantic versions. */
  runtimeVersionRequirement: string;
  /** Existing local model package path. Browser/server runtimes reject it. */
  sourcePath: string;
  /** Exact tokenizer identifier. */
  tokenizer: string;
  /** Exact immutable upstream revision. */
  upstreamRevision: string;
}
