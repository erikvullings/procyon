/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { ChecksumEntryDtoChecksums } from './checksumEntryDtoChecksums.ts';
import type { LocationDto } from './locationDto.ts';

/**
 * One entry's computed checksums.
 */
export interface ChecksumEntryDto {
  /** Digests keyed by lower-case algorithm name (`sha256`, `blake3`, …). */
  checksums: ChecksumEntryDtoChecksums;
  /**
     * Why this entry could not be hashed, when it could not be.
     * @nullable
     */
  error?: string | null;
  /** Location of the hashed entry. */
  location: LocationDto;
  /** Path relative to the selection's common root. */
  relativePath: string;
  /**
     * Bytes hashed.
     * @minimum 0
     */
  size: number;
}
