/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */

/**
 * Predicates the backend re-validates before invoking an action (spec §18).
 */
export interface ActionContextRequirementsDto {
  /** Whether the action's feature is implemented yet. */
  featureAvailable: boolean;
  /** At least one entry must be selected. */
  requiresSelection: boolean;
  /** Exactly one entry must be selected. */
  requiresSingleSelection: boolean;
}
