/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { DirectoryViewConfigurationDto } from './directoryViewConfigurationDto.ts';
import type { LocationDto } from './locationDto.ts';
import type { NavigationHistoryDto } from './navigationHistoryDto.ts';

/**
 * A single tab: a location, its navigation history and its view configuration.
 */
export interface TabStateDto {
  /** Back/forward navigation history for this tab. */
  history: NavigationHistoryDto;
  /** Stable identifier for this tab. */
  id: string;
  /** The location currently shown in this tab. */
  location: LocationDto;
  /** Whether this tab is pinned. */
  pinned: boolean;
  /**
     * An optional user-facing title override for this tab.
     * @nullable
     */
  titleOverride?: string | null;
  /** Whether this tab belongs only to the current application session. */
  transient?: boolean;
  /** Persisted view configuration (sort, columns, filters) for this tab. */
  view: DirectoryViewConfigurationDto;
}
