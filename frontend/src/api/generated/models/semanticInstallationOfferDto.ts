/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { SemanticComponentDisclosureDto } from './semanticComponentDisclosureDto.ts';
import type { SemanticModelIdentityDto } from './semanticModelIdentityDto.ts';
import type { SemanticProfileDto } from './semanticProfileDto.ts';

/**
 * Complete signed first-install disclosure awaiting explicit consent.
 */
export interface SemanticInstallationOfferDto {
  /** Immutable signed catalog revision. */
  catalogRevision: string;
  /** Worker, runtime, and model disclosures. */
  components: SemanticComponentDisclosureDto[];
  /** Proposed semantic-data root. */
  dataRoot: string;
  /** Whether inference and semantic data remain on this device. */
  embeddingsStayLocal: boolean;
  /** Stable local-only privacy disclosure. */
  localOnlyDisclosure: string;
  /**
     * Free bytes reserved beyond installed-size estimates.
     * @minimum 0
     */
  minimumFreeSpaceReserveBytes: number;
  /** Opaque identity accepted by the installation action. */
  offerId: string;
  /** Selected abstract profile. */
  profile: SemanticProfileDto;
  /** Exact model revision resolved by the catalog. */
  resolvedModel: SemanticModelIdentityDto;
}
