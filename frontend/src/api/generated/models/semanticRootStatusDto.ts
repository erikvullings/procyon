/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { LocationDto } from './locationDto.ts';
import type { SemanticEligibilityOverrideStatusDto } from './semanticEligibilityOverrideStatusDto.ts';
import type { SemanticEligibilityReasonCountDto } from './semanticEligibilityReasonCountDto.ts';
import type { SemanticExclusionStatusDto } from './semanticExclusionStatusDto.ts';
import type { SemanticRootAvailabilityDto } from './semanticRootAvailabilityDto.ts';

/**
 * Safe status for one globally enrolled root.
 */
export interface SemanticRootStatusDto {
  /** Persisted vocabulary identifiers. */
  attachedVocabularyIds: string[];
  /** Current source reachability. */
  availability: SemanticRootAvailabilityDto;
  /** Fixed reason-based eligibility overrides. */
  eligibilityOverrides: SemanticEligibilityOverrideStatusDto[];
  /** Last available skip-reason counts. */
  eligibilityReasonCounts: SemanticEligibilityReasonCountDto[];
  /** Explicit exclusions. */
  exclusions: SemanticExclusionStatusDto[];
  /** Stable path-independent root identity. */
  id: string;
  /**
     * Last indexed generation.
     * @minimum 0
     */
  indexedGeneration: number;
  /** Provider-neutral location already visible to the user. */
  location: LocationDto;
  /**
     * Last complete catalog reconciliation generation.
     * @minimum 0
     */
  reconciliationGeneration: number;
  /** Whether descendants inherit consent. */
  recursive: boolean;
  /** Whether a provider supplied stable identity. */
  stableIdentityVerified: boolean;
  /** Workspaces attached to this global consent. */
  workspaceReferences: string[];
}
