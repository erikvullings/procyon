/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { SyncModeDto } from './syncModeDto.ts';

/**
 * Proposes a sync plan from a comparison's current results
 * (`POST /api/v1/comparisons/{comparisonId}/sync-plan`).
 *
 * Generating a plan never touches a filesystem (spec §35): it only reads
 * the comparison's already-computed results.
 */
export interface GenerateSyncPlanRequestDto {
  /** Which side is authoritative. */
  mode: SyncModeDto;
}
