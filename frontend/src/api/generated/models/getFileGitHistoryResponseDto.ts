/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { GitLogEntryDto } from './gitLogEntryDto.ts';

/**
 * A file's git history: empty when the file is outside a git working tree, on a non-local
 * provider, or not yet tracked by any commit.
 */
export interface GetFileGitHistoryResponseDto {
  /** Commits touching the file, newest first, up to a server-side cap. */
  commits: GitLogEntryDto[];
}
