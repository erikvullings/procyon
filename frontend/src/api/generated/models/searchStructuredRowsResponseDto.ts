/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { StructuredRowDto } from './structuredRowDto.ts';

/**
 * One bounded search page and an optional continuation cursor.
 */
export interface SearchStructuredRowsResponseDto {
  /** Whether the source indexer reached EOF. */
  indexingComplete: boolean;
  /** Matching logical records. */
  matches: StructuredRowDto[];
  /**
     * Continuation row for the next bounded search page.
     * @minimum 0
     * @nullable
     */
  nextCursor?: number | null;
}
