/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { ApplicationErrorCode } from './applicationErrorCode.ts';
import type { ApplicationErrorDtoDetails } from './applicationErrorDtoDetails.ts';

/**
 * A structured, user-facing error, never a raw OS error string (spec §8).
 */
export interface ApplicationErrorDto {
  /** A stable, machine-readable error code. */
  code: ApplicationErrorCode;
  /**
     * Additional, code-specific structured context.
     * @nullable
     */
  details?: ApplicationErrorDtoDetails;
  /** A user-readable description, never a raw OS error. */
  message: string;
  /** Correlates this error with the request that produced it. */
  requestId: string;
}
