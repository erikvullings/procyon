/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { RagRetrievalStrategyDto } from './ragRetrievalStrategyDto.ts';
import type { RagScopeDto } from './ragScopeDto.ts';

/**
 * Confirms generation against an inspected evidence set.
 */
export interface GenerateRagAnswerRequestDto {
  /** Explicit opt-in to distinguishable model-only knowledge. */
  allowModelKnowledge: boolean;
  /**
     * Server-issued ephemeral conversation to continue.
     * @nullable
     */
  conversationId?: string | null;
  /** Fingerprint returned by preview. */
  expectedRetrievalFingerprint: string;
  /** Selected saved generation profile. */
  profileId: string;
  /** Exact question used during preview. */
  question: string;
  /** Retrieval strategy confirmed during preview. */
  retrievalStrategy?: RagRetrievalStrategyDto;
  /** Visible authorized evidence scope. */
  scope: RagScopeDto;
}
