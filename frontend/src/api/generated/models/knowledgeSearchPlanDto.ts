/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { KnowledgeExcludedFieldDto } from './knowledgeExcludedFieldDto.ts';
import type { KnowledgePlannedSearchDto } from './knowledgePlannedSearchDto.ts';
import type { KnowledgeRetrievalModeDto } from './knowledgeRetrievalModeDto.ts';
import type { KnowledgeScopeDto } from './knowledgeScopeDto.ts';
import type { KnowledgeSearchOptionsDto } from './knowledgeSearchOptionsDto.ts';

/**
 * Deterministic inspectable plan.
 */
export interface KnowledgeSearchPlanDto {
  /**
     * Number of authorized indexed sources inside the scope.
     * @minimum 0
     */
  authorizedSources: number;
  /** Answer-only fields present in the request and excluded from retrieval. */
  excludedFromRetrieval: KnowledgeExcludedFieldDto[];
  /** Requested retrieval mode. */
  mode: KnowledgeRetrievalModeDto;
  /**
     * Expansions omitted because of query-size or search-count bounds.
     * @minimum 0
     */
  omittedSearches: number;
  /** Validated resource and evidence options. */
  options: KnowledgeSearchOptionsDto;
  /**
     * Resolved authorized scope this plan executes against.
     *
     * This is the effective scope after root selectors, DSL scope selectors,
     * and host authorization were applied, not the requested scope.
     */
  scope: KnowledgeScopeDto;
  /** Whether retrieval ranks exactly this scope rather than a superset. */
  scopeIsExact: boolean;
  /** User-visible scope label. */
  scopeLabel: string;
  /** Stable bounded source searches. */
  searches: KnowledgePlannedSearchDto[];
  /** Original ordered subjects. */
  subjects: string[];
  /** Planner identity. */
  version: string;
}
