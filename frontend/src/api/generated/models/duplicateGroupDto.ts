/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { HardlinkClusterDto } from './hardlinkClusterDto.ts';
import type { LocationDto } from './locationDto.ts';

/**
 * A set of byte-identical files.
 */
export interface DuplicateGroupDto {
  /** Byte-identical files with distinct identities — the true duplicates. */
  distinctLocations: LocationDto[];
  /** Full-content digest shared by every member. */
  fullHash: string;
  /**
     * Paths that are the same file through a hardlink. Presented separately
     * because deleting one of them reclaims nothing.
     */
  hardlinkClusters: HardlinkClusterDto[];
  /**
     * Bytes reclaimable by keeping one copy of the content.
     * @minimum 0
     */
  reclaimableBytes: number;
  /**
     * Byte size shared by every member.
     * @minimum 0
     */
  size: number;
}
