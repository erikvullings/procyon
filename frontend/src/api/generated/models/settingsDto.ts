/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { ConflictPolicyDto } from './conflictPolicyDto.ts';
import type { DateFormatDto } from './dateFormatDto.ts';
import type { DefaultPaneLayoutDto } from './defaultPaneLayoutDto.ts';
import type { FavouriteLocationDto } from './favouriteLocationDto.ts';
import type { LanguageDto } from './languageDto.ts';
import type { MultiRenamePresetDto } from './multiRenamePresetDto.ts';
import type { SavedSearchDto } from './savedSearchDto.ts';
import type { SettingsDtoColumnWidths } from './settingsDtoColumnWidths.ts';
import type { SettingsDtoKeybindings } from './settingsDtoKeybindings.ts';
import type { SettingsDtoPluginSettings } from './settingsDtoPluginSettings.ts';
import type { SettingsDtoRecentLocationsByWorkspace } from './settingsDtoRecentLocationsByWorkspace.ts';
import type { SizeFormatDto } from './sizeFormatDto.ts';
import type { ThemeDto } from './themeDto.ts';

/**
 * Versioned global settings. Live workspace content is deliberately absent.
 */
export interface SettingsDto {
  /** Column widths in CSS pixels, keyed by column id, shared by every tab and pane. */
  columnWidths: SettingsDtoColumnWidths;
  /** Confirm permanent deletion. */
  confirmPermanentDelete: boolean;
  /** Timestamp presentation. */
  dateFormat: DateFormatDto;
  /** Columns inherited by new tabs. */
  defaultColumns: string[];
  /** Default operation conflict policy. */
  defaultConflictPolicy: ConflictPolicyDto;
  /** Layout inherited by new workspaces. */
  defaultPaneLayout: DefaultPaneLayoutDto;
  /** Locations inherited by new panes. */
  defaultStartLocations: string[];
  /**
     * Optional text-editor command for `core.edit`; `None` uses the platform default.
     * @nullable
     */
  editorCommand?: string | null;
  /** Enabled plugin identifiers. */
  enabledPlugins: string[];
  /** User-managed named locations, in the order shown by the favourites menu. */
  favouriteLocations: FavouriteLocationDto[];
  /**
     * Base font size in CSS pixels.
     * @minimum 0
     */
  fontSize: number;
  /** Directory-entry icon set: `"generic"` for the built-in glyphs, or a discovered plugin's id. */
  iconTheme: string;
  /** Action-to-shortcut mappings. */
  keybindings: SettingsDtoKeybindings;
  /** UI language. */
  language: LanguageDto;
  /** User-named reusable multi-rename configurations. */
  multiRenamePresets: MultiRenamePresetDto[];
  /**
     * Maximum concurrent operations.
     * @minimum 0
     */
  operationConcurrency: number;
  /** Non-secret plugin settings keyed by plugin identifier. */
  pluginSettings: SettingsDtoPluginSettings;
  /** Recently visited locations per workspace, newest first. */
  recentLocationsByWorkspace: SettingsDtoRecentLocationsByWorkspace;
  /**
     * Directory row height in CSS pixels.
     * @minimum 0
     */
  rowHeight: number;
  /** Durable smart folders, ordered with pinned searches first by the UI. */
  savedSearches: SavedSearchDto[];
  /**
     * On-disk schema version.
     * @minimum 0
     */
  schemaVersion: number;
  /** Show hidden entries by default. */
  showHiddenFiles: boolean;
  /** Size presentation. */
  sizeFormat: SizeFormatDto;
  /**
     * Optional terminal command.
     * @nullable
     */
  terminalCommand?: string | null;
  /** Application theme. */
  theme: ThemeDto;
}
