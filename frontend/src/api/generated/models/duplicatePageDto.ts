/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { DuplicateGroupDto } from './duplicateGroupDto.ts';
import type { DuplicateStatsDto } from './duplicateStatsDto.ts';
import type { LocationDto } from './locationDto.ts';

/**
 * A bounded page of a duplicate scan's grouped results
 * (`GET /api/v1/duplicate-scans/{scanId}`).
 */
export interface DuplicatePageDto {
  /** Groups in this page. */
  groups: DuplicateGroupDto[];
  /** Whether another page exists. */
  hasMore: boolean;
  /**
     * Whether the scan stopped because it was cancelled. A cancelled scan
     * reports no groups, so this distinguishes "none found" from "did not
     * finish looking".
     */
  isCancelled: boolean;
  /** Whether the scan has finished. */
  isComplete: boolean;
  /**
     * Requested page size after server-side clamping.
     * @minimum 0
     */
  limit: number;
  /**
     * Requested zero-based offset.
     * @minimum 0
     */
  offset: number;
  /** Roots the scan covered. */
  roots: LocationDto[];
  /** The scan this page belongs to. */
  scanId: string;
  /** How much work each stage performed. */
  stats: DuplicateStatsDto;
  /**
     * Total groups found.
     * @minimum 0
     */
  total: number;
  /**
     * Number of files that had to be skipped.
     * @minimum 0
     */
  warningsCount: number;
}
