# olecfsdk-derive

Procedural derives used by [`olecfsdk`](https://crates.io/crates/olecfsdk) to
generate bounded, symmetric binary read/write/size implementations.

This crate is an implementation companion to `olecfsdk`; most users should
depend on `olecfsdk`, which re-exports the derives.

## License

MIT OR Apache-2.0
